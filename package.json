{
  "name": "arsip-buku-tanah",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm --workspace frontend run build",
    "vercel-build": "npm --workspace frontend run build && node scripts/sync-vercel-output.js",
    "setup:prod": "npm --workspace backend run setup:prod"
  },
  "engines": {
    "node": ">=20"
  }
}
