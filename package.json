{
  "name": "arsip-buku-tanah",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "dependencies": {
    "mysql2": "^3.15.2"
  },
  "scripts": {
    "build": "npm --workspace frontend run build && node scripts/sync-vercel-output.js",
    "vercel-build": "npm --workspace frontend run build && node scripts/sync-vercel-output.js",
    "setup:prod": "npm --workspace backend run setup:prod"
  },
  "engines": {
    "node": ">=20"
  }
}
